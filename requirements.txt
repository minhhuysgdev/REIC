# REIC - Retrieval-Enhanced Intent Classification
# Core (TF-IDF fallback - không cần tải model)
numpy>=1.24.0
scikit-learn>=1.3.0

# Dense retrieval (optional, tải model ~100MB lần đầu)
sentence-transformers>=2.2.0

# Optional: LLM reranker (OpenAI)
openai>=1.0.0

# Baselines (BERT, Qwen)
transformers>=4.35.0
torch>=2.0.0
accelerate>=0.24.0

# Notebook
jupyter>=1.0.0
pandas>=2.0.0
matplotlib>=3.7.0

# Dev
pytest>=7.0.0
